/**
 * Marketplace / multivendor defaults.
 * Commission is taken from each vendor's product line subtotal (sale amount), not shipping.
 */
module.exports = {
  /** Default platform commission when creating a new vendor (percent). */
  DEFAULT_VENDOR_COMMISSION_PERCENT: 3.0,
};
